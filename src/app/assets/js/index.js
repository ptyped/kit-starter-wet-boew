/**
 * Add any desired JavaScript here. It will be processed with Webpack, and
 * supports NodeJS require and/or import statements of local scripts, NPM
 * packages, and your templates.
 **/
import "./autoStoreData"

if (window.console && window.console.info) {
    window.console.info('pType Kit - use in production at your own risk!')
}